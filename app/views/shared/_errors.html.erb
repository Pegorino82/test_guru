<% if resource.errors.present? %>
  <div>
    <p class="h4"><%= t('.header', count: resource.errors.count) %></p>
    <ul>
      <% resource.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>