<h3><%= t('.header') %></h3>

<p><%= link_to t('.create'), new_admin_test_path %></p>

<table>
  <thead style="text-align: center;">
    <tr>
      <td><%= t('.title') %></td>
      <td><%= t('.level') %></td>
      <td><%= t('.questions') %></td>
    </tr>
  </thead>

  <%= render @tests %>
</table>
